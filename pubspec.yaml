name: custom_scaffold
description: A modern, customizable Scaffold widget with dark/light themes, background images, gradients, glassmorphic blur, and loading overlays.
version: 2.0.0
repository: https://github.com/Earbaj/custom_scaffold.git
issue_tracker: https://github.com/Earbaj/custom_scaffold/issues

topics:
  - scaffold
  - ui
  - glassmorphism
  - background
  - theme

environment:
  sdk: ^3.1.0
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
